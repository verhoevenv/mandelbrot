{
  "name": "mandelbrot-js",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && tsc && cp src/static/* dist",
    "serve": "npm run build && python3 -m http.server -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.6.2"
  }
}
